# KCFX (Demo version)

A demo web app that simulates gold (XAU/USD) strategy analysis on a 1-hour timeframe,
built with Flask and Socket.IO.

## Run locally
```bash
pip install -r requirements.txt
python main.py
